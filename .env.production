# Production build config for the AWS adapter (from `cdk deploy` outputs).
# These are PUBLIC client identifiers — they ship in the browser bundle either
# way, so committing them is fine. The Cognito app client has no secret.
# Vite loads this automatically for `npm run build` (production mode).
VITE_AWS_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=us-east-1_TK5lsOuO8
VITE_COGNITO_CLIENT_ID=1s5hh6lap1ch6ldc7hgv9kthqc
VITE_API_BASE_URL=https://i66f543akd.execute-api.us-east-1.amazonaws.com
